<!--logs.wxml-->
<view class="container log-list" bind:touchstart="tapLogs">
  <block wx:for="{{logs}}" wx:for-item="log" wx:key="log">
    <text bindtap="tapLogs" class="log-item">{{index + 1}}. {{log}}</text>
  </block>
</view>
